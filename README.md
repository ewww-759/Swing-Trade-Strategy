# Swing-Trade-Strategy
Separates binary go/no-go gates (liquidity, trend structure) from a 0–100 quality score built on four weighted layers — position, momentum, trigger, and volume — with an accumulation/distribution penalty and trend-strength damping. Every trade risks a fixed percentage of capital scaled by stop distance.
